name: Create UDE (OIDC + Async)

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  create-ude:
    runs-on: ubuntu-latest
    timeout-minutes: 15   # since async returns immediately

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Power Platform CLI
        uses: microsoft/powerplatform-actions/actions-install@v1.9.1

      # 🚀 Create UDE Environment Asynchronously
      - name: Create UDE Environment (Async)
        run: |
          pac admin create \
            --name "fotest-ude-dev01" \
            --type Sandbox \
            --region unitedstates \
            --currency USD \
            --language 1033 \
            --domain ude-dev01 \
            --templates D365_FinOps_Finance \
            --template-metadata-json '{
              "PostProvisioningPackages": [
                {
                  "applicationUniqueName": "msdyn_FinanceAndOperationsProvisioningAppAnchor",
                  "parameters": "DevToolsEnabled=true|DemoDataEnabled=true"
                }
              ]
            }' \
            --async

      # 📋 Optional: Show all environments
      - name: List Environments
        run: pac admin list
